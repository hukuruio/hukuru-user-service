---
kind: pipeline
name: default

steps:
  - name: build
    image: hukuruio/docker-compose
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    commands:
      - make

volumes:
  - name: dockersock
    host:
      path: /var/run/docker.sock
